spring.application.name=fatecads

# Configurações do banco de dados
spring.datasource.url=jdbc:postgresql://localhost:5432/fatecads_db
spring.datasource.username=postgres
spring.datasource.password=123456

# Atualização automática do esquema do banco de dados
spring.jpa.hibernate.ddl-auto=update

# Mostrar as consultas SQL no console
spring.jpa.show-sql=true

# Não formatar as consultas SQL
spring.jpa.properties.hibernate.format_sql=false

# Configuração da porta do servidor
server.port=3000

# Configuracoes de email
app.mail.mode=smtp
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=oliveiratrinca@gmail.com
spring.mail.password=bztidfhimwgohwjs
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Configuracoes JWT para recuperacao de senha
app.jwt.secret=troque-esta-chave-para-uma-chave-segura-com-mais-de-32-chars
app.jwt.issuer=fatecads
app.jwt.expiration-minutes=15

# URL base do link de redefinicao
app.reset.base-url=http://localhost:3000/redefinir-senha